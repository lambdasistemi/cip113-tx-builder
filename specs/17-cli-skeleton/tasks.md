# Issue #17 Tasks

## Slice 1 — CLI Skeleton

- [X] T017-S1 Add the `cip113-cli` executable component and CLI modules under `exe/`.
- [X] T017-S1 Implement optparse dispatch, global `--json`, documented exit codes, and stub handlers.
- [X] T017-S1 Implement `UTxOProvider` and the offline JSON provider.
- [X] T017-S1 Prove formatting, linting, help smoke, and build behavior via `./gate.sh`.
- [X] T017-S1 Commit the slice with the required `Tasks: T017-S1` trailer.
